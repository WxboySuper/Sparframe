# Application extensions

The core repository intentionally ships no extensions. An application created
with `npx sparframe init` owns its `extensions/` directory and may add domain,
integration, authentication, persistence, or presentation packages there.

Use the [extension authoring guide](../docs/EXTENSION_AUTHORING.md) to add one.
